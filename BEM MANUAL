#include <stdio.h>

int main() {
    int T;
    int A, B, C, D, E;
    int corretos = 0;

    scanf("%d", &T);

    scanf("%d %d %d %d %d", &A, &B, &C, &D, &E);

         if (A == T) corretos++;
          if (B == T) corretos++;
            if (C == T) corretos++;
             if (D == T) corretos++;
              if (E == T) corretos++;

    
    printf("%d\n", corretos);

    return 0;
}
