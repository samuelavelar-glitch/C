#include <stdio.h> 
int main(){ 
    
    int i=0 , j=60 ,r1=60 , r2=1; 
    for (i = 1, j = 60; j >= 0; i += 3, j -= 5){

    printf("I=%d J=%d\n", i, j);

    } 
    return 0;
    
}
  \\nao copiar\\
  \\ versao bugada precisa refazer e modificar\\
    #include <stdio.h> 
int main(){ 
    
    int i=0 , j=60 ,r1=60 , r2=1; 
for (i = 1, j = 60; j >= 0; i += 3, j -= 5) {

        printf("I=%d J=%d\n", i, j);
    r1 = i + 3 ;
    for( ; j > 0 ; j--){ 
        r2 = j - 5; 
   
    if (i < 36 || j > 0 && j % 2 == 0 ){ 
        printf("I=%d J=%d\n",r1 , r2);
        } 
  
    if (j == 0 ){ 
        printf("I=%d J=%d\n",r1,r2); 
        
    } 
        
    }
    
    
} 
    return 0;
    
}
