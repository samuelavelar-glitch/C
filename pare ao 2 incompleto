#include <stdio.h>
 
int main() {
 
    int n , i;
 
   scanf("%d", &n);
   
    for( i = 1 ; i < n ; i++ ){
        
        if(i % 2 !=1 ){
        i = i*i;
        printf(" %d^2 = %d\n",i);
        }
    }
    
 
    return 0;
}
