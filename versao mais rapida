#include <stdio.h>
 
int main() {
  int i ,T;
  int palpite , corretos=0;
    scan("%d",&T);
  for(i = 0 ; i<5 ; i++ ){
        scanf("%d",&palpite );
       if( palpite == T ){
           corretos++;
       }
  }
    
   printf("%d\n", corretos);
 
 
    return 0;
}
