#include <stdio.h>

int main() {

   int x , y , s1=0 , s2=0 , st=0 ,i;


    if (scanf("%d %d", &x, &y) != 2) return 1;
      if(x>0){


     for(i=0 ; i<x ; i++){

         if( i % 2 == !0 ){
           s1 += i;

         }

     }
          }else{

        for (int i = 0; i > x; i--) {
            s1 += i;
        }

      }

     if (y>0){

         for(i=0; i<y ; i++){

        if(i % 2 == !0 ){
           s2 += i;
         } else {
        for (int i = 0; i > x; i--) {
            s1 += i;
        }
    }


     }
    }
        if (s1 > 0 || s2 > 0){
            
        st = s1 - s2;
        }else{
            st = s1 + s2;
        }
         printf("%d\n", st);

    return 0;
}
