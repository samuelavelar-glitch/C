#include <stdio.h>

int main() {

    int dias1 ,horas1 , minutos1 , segundos1, total1 ;
     int dias2 ,horas2 , minutos2 , segundos2 , total2 , differenca ;
     int dias , horas , minutos , segundos;
 
        scanf("Dia %d", &dias1);
        scanf(" %d : %d : %d", &horas1, &minutos1, &segundos1);

        scanf(" Dia %d", &dias2);
        scanf(" %d : %d : %d", &horas2, &minutos2, &segundos2);

        total1 = segundos1 + ( minutos1 * 60 ) + ( horas1 * 3600) + (dias1 * 86400);

       total2 = segundos2 + ( minutos2* 60 ) + ( horas2 * 3600) + (dias2 * 86400);

     differenca = total2 - total1;

    dias = differenca / 86400;
      differenca %=86400;
     horas = differenca / 3600;
      differenca %=3600;
         minutos= differenca / 60;
        segundos = differenca % 60;
    printf("%d dia(s)\n", dias);
    printf("%d hora(s)\n", horas);
    printf("%d minuto(s)\n", minutos);
    printf("%d segundo(s)\n", segundos);

    return 0;
}
