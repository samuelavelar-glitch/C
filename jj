#include <stdio.h>
 
int main() {
 
  int i , x , m; 
     scanf("%d",&x);
     for(i = 1 ; i<=10; i++){
         m = i * x;
         printf("%d x %d = %d\n",i,x,m);
         
     }
   
    return 0;
}
